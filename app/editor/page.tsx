import { EditorWorkspace } from "@/components/editor-workspace";

export const metadata = {
  title: "Editor",
  description: "Write and preview markdown with a simple layout.",
};

export default function EditorPage() {
  return <EditorWorkspace />;
}
